#include "monty.h"

/**
  *f_nop -> does nothing.
  *@head: the stack head.
  *@counter: line counter.
  *Return: no return.
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
